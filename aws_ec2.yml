plugin: amazon.aws.aws_ec2
regions:
  - "eu-west-2"

filters:
 instance-state-name: running

keyed_groups:
  - key: tags['dev']
    prefix: "dev"

  - key: tags['OS']
    prefix: "os"

  - key: tags['monitor']
    prefix: "monitor"